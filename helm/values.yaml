# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  #  repository: pauldon
  repository: ghcr.io/pauldon2
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v0.0.3-a3af940"
secret:
  name: "ptbot"
  env: "TELE_TOKEN"
  key: "token"
